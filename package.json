{
  "name": "presskit",
  "version": "2.0.0",
  "description": "Pixelnest Studio Presskit",
  "license": "UNLICENSED",
  "private": true,
  "repository": "pixelnest/presskit",
  "author": {
    "name": "Pixelnest Studio",
    "url": "pixelnest.io"
  },
  "scripts": {
    "start": "presskit build -L --watch data",
    "deploy": "presskit build -B /presskit/ -DL data && cp -r redirections/* build/ && gh-pages --dist build --branch build"
  },
  "dependencies": {
    "gh-pages": "^0.12.0",
    "presskit": "^0.11.0"
  },
  "devDependencies": {}
}
